import { ReziseriToStringPipe } from './reziseri-to-string.pipe';

describe('ReziseriToStringPipe', () => {
  it('create an instance', () => {
    const pipe = new ReziseriToStringPipe();
    expect(pipe).toBeTruthy();
  });
});
