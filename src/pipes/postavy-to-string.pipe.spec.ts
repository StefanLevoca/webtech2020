import { PostavyToStringPipe } from './postavy-to-string.pipe';

describe('PostavyToStringPipe', () => {
  it('create an instance', () => {
    const pipe = new PostavyToStringPipe();
    expect(pipe).toBeTruthy();
  });
});
